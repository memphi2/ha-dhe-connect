# Runtime and validation dependencies for local development and release checks.

# Runtime dependency used by the integration.
# Keep this compatible with the Home Assistant 2026.6 fixture environment.
aiohttp>=3.13.5,<4

# Home Assistant integration test/runtime plugin.
homeassistant==2026.6.0
# Install pytest-homeassistant-custom-component separately with --no-deps until its
# metadata catches up with Home Assistant 2026.6. The support dependencies below
# mirror pytest-homeassistant-custom-component 0.13.335 except for its Home
# Assistant transitive pin.
aiohasupervisor==0.4.3
ast-serialize==0.3.0
coverage==7.14.0
freezegun==1.5.5
license-expression==30.4.3
mock-open==1.4.0
numpy==2.3.2
paho-mqtt==2.1.0
pipdeptree==2.26.1
pydantic==2.13.4
pytest-aiohttp==1.1.0
pytest-asyncio==1.3.0
pytest-freezer==0.4.9
pytest-github-actions-annotate-failures==0.4.0
pytest-picked==0.5.1
pytest-socket==0.7.0
pytest-timeout==2.4.0
pytest-unordered==0.7.0
pytest-xdist==3.8.0
requests-mock==1.12.1
respx==0.23.1
syrupy==5.2.0
tqdm==4.67.1
unidiff==0.7.5

# Test tooling.
# Keep these pins aligned with the HA 2026.6 fixture helper range.
pytest==9.0.3
pytest-cov==7.1.0

# Quality gates.
mypy>=1.20,<2
ruff>=0.15,<0.16

# Test/runtime helpers.
voluptuous>=0.14,<1
zeroconf==0.149.16
